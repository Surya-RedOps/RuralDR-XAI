# RuralDR-XAI Configuration Template

# Database (MySQL production, SQLite fallback automatically active when unreachable)
DATABASE_URL=mysql+pymysql://ruraldr_user:ruraldr_password_sih@localhost:3307/ruraldr_db

# Security & JWT Token Authentication
JWT_SECRET_KEY=ruraldr-xai-super-secret-production-key-sih26038
JWT_ALGORITHM=HS256
JWT_EXPIRY_MINUTES=1440

# AWS S3 Storage (Optional: Falls back to secure local directory data/uploads)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=ap-south-1
AWS_S3_BUCKET=ruraldr-xai-retinal-scans

# Frontend API URL
VITE_API_URL=http://localhost:8000
